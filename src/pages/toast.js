import React from "react";
import { ToastContainer } from "react-bootstrap";
import Toast from "react-bootstrap/Toast";
import MyToast from "../../components/MyToast";

const t = ({ bg, title, message, position }) => {
  return <MyToast />;
};

export default t;
